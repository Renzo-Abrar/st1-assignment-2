# Part E: Compare Human and AI Versions

| Question                | Human Version | AI Version 
|-------------------------|---|---|
| **Easy to understand?** | Yes, clear naming and structured functions. | Yes, very simple and minimal.
| **Runs successfully?** | Yes, executes without any errors. | Yes, executes without any errors. |
| **Uses only required features?** | Yes, strictly uses basic lists, dicts, and functions. | Yes, uses basic lists, dicts, and functions as requested. |
| **Adds assumptions?** | Assumes users will manually format string inputs correctly. | Assumes valid data will always be passed in (no input validation). |
| **Handles errors?** | Includes basic check to raise `ValueError` for empty patient names. | No error handling; accepts blank or invalid inputs freely. |
| **Could I explain it?** | Yes, clear logic structure and standard function setup. | Yes, basic loop and a dictionary to a list. |